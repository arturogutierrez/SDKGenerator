import 'package:json_annotation/json_annotation.dart';

part '<%- api.name.toLowerCase() %>_models.g.dart';

<% for(var d in api.datatypes) { var datatype = api.datatypes[d] %>
<%- makeDatatype(datatype, api) %><% } %>
